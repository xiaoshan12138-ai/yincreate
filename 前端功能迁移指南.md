# 前端功能迁移指南

本文档整理了当前平台已实现的核心功能模块代码，方便迁移到新平台。

---

## 目录

1. [全局配置与常量](#1-全局配置与常量)
9. [后端模型拉取与渲染](#2-后端模型拉取与渲染) ⭐ 核心
10. [用户文件上传功能](#3-用户文件上传功能)
11. [对话交互功能](#4-对话交互功能)
12. [发送按钮与暂停功能](#5-发送按钮与暂停功能)
13. [请求构建功能](#6-请求构建功能)
14. [结果渲染功能](#7-结果渲染功能)
15. [模型选择交互](#8-模型选择交互)
16. [HTML 结构参考](#9-html-结构参考)

---

## 1. 全局配置与常量

```javascript
// ======================== API 配置 ========================
const API_CONFIG = {
    BASE_URL: 'http://192.168.31.243:8003/api/v1',
};

// ======================== Token Switch 配置 ========================
const VENDOR_B_API_KEY = 'ErtveAQybj1XCVRsncebuiIYzTxUV0tganVf4bMijr5SKVzU';

// ======================== 历史记录存储 Key ========================
const HISTORY_STORAGE_KEY = 'generated_files_history';

// ======================== 全局状态 ========================
let globalModels = {
    image_models: [],
    video_models: [],
    voices: []
};
let uploadedFiles = [];
```

### vendor 字段提交规则

```javascript
// vendor_a（腾讯云）→ 提交 name
// vendor_b（Token Switch）→ 提交 id
const submitModel = (vendor === 'vendor_b' && window.__selectedModelId)
    ? window.__selectedModelId
    : modelName;
```

### 1.4 全局 Toast 提示 ⚠️ 必须迁移

> **所有模块依赖此函数**：错误提示、成功提示、警告信息都通过 `showToast()` 显示

```javascript
/**
 * 全局 Toast 提示组件
 * @param {string} msg - 提示消息
 * @param {string} type - 类型: 'info' | 'success' | 'warning' | 'error'
 */
function showToast(msg, type = 'info') {
    // 当前使用 alert 简单实现，新平台可替换为美观的 toast 组件
    console.log(`[${type.toUpperCase()}] ${msg}`);
    alert(msg);
}

// 使用示例：
// showToast('获取模型列表失败', 'error');
// showToast('生成成功！', 'success');
// showToast('无法连接后端服务', 'warning');
```

### 1.5 全局参数获取 ⚠️ 必须迁移

> **模型拉取、请求构建都依赖此函数**：获取当前场景类型、比例、分辨率

```javascript
/**
 * 获取当前用户选择的参数（场景类型、比例、分辨率）
 * @returns {Object} { sceneType: 'image'|'video', ratio: string, resolution: string }
 */
function getCurrentParams() {
    // 判断当前激活的场景面板
    const scene = Array.from(document.querySelectorAll('.scene-controls')).find(s => {
        return window.getComputedStyle(s).display !== 'none';
    });

    let sceneType = 'image';
    if (scene) {
        if (scene.classList.contains('video-scene')) sceneType = 'video';
        else if (scene.classList.contains('digital-human-scene')) sceneType = 'digital-human';
    }

    let ratio = '1:1';
    let resolution = '2K';

    if (sceneType === 'image') {
        const activeRatio = document.querySelector('#imageRatioResDropdown .ratio-item.active');
        const activeRes = document.querySelector('#imageRatioResDropdown .resolution-btn.active');
        ratio = activeRatio ? activeRatio.getAttribute('data-ratio') : '1:1';
        resolution = activeRes ? activeRes.getAttribute('data-res') : '2K';
    } else if (sceneType === 'video') {
        const activeRatio = document.querySelector('#videoRatioResDropdown .ratio-item.active');
        const activeRes = document.querySelector('#videoRatioResDropdown .resolution-btn.active');
        ratio = activeRatio ? activeRatio.getAttribute('data-ratio') : '16:9';
        resolution = activeRes ? activeRes.getAttribute('data-res') : '2K';
    }

    return { sceneType, ratio, resolution };
}

// 使用示例：
// const params = getCurrentParams();
// console.log(params);  // { sceneType: 'image', ratio: '16:9', resolution: '1080P' }
```

---

## 1.6 UI 交互初始化（下拉菜单、按钮切换）

### 1.6.1 下拉菜单通用交互

> **所有 `.control-dropdown` 的基础行为**：点击展开/收起、选项点击更新文本

```javascript
/**
 * 初始化所有控制下拉菜单的通用交互
 * - 点击触发器展开/收起菜单
 * - 点击外部关闭所有菜单
 * - 选项点击后更新显示文本
 */
function initDropdowns() {
    const dropdowns = document.querySelectorAll('.control-dropdown');

    dropdowns.forEach(dropdown => {
        const menu = dropdown.querySelector('.dropdown-menu');
        if (!menu) return;

        // 点击触发器：切换菜单显隐
        dropdown.addEventListener('click', (e) => {
            e.stopPropagation();
            const isOpen = menu.style.display === 'block';
            document.querySelectorAll('.dropdown-menu').forEach(m => m.style.display = 'none');
            menu.style.display = isOpen ? 'none' : 'block';
        });
    });

    // 点击页面其他区域：关闭所有菜单
    document.addEventListener('click', () => {
        document.querySelectorAll('.dropdown-menu').forEach(menu => menu.style.display = 'none');
    });

    // 通用下拉选项点击处理
    document.querySelectorAll('.dropdown-item').forEach(item => {
        item.addEventListener('click', (e) => {
            e.stopPropagation();
            const menu = item.closest('.dropdown-menu');
            const parentDrop = item.closest('.control-dropdown');

            if (parentDrop) {
                const labelSpan = parentDrop.querySelector('span:first-child');
                if (labelSpan && !labelSpan.querySelector('i')) {
                    labelSpan.textContent = item.textContent.trim();
                }
            }

            if (menu) {
                menu.querySelectorAll('.dropdown-item').forEach(opt => opt.classList.remove('active'));
                item.classList.add('active');
                menu.style.display = 'none';
            }
        });
    });
}
```

### 1.6.2 比例与分辨率按钮切换

> **比例(1:1/16:9等)和分辨率(720P/1080P/2K)按钮**的 active 状态管理 + 标签同步更新

```javascript
/**
 * 初始化比例选择器和分辨率按钮的交互
 * - 点击设置 active 状态
 * - 同步更新上级下拉菜单的显示文本
 */
function initRatioButtons() {
    // 比例按钮（1:1, 4:3, 3:4, 16:9, 9:16）
    document.querySelectorAll('.ratio-item').forEach(btn => {
        btn.addEventListener('click', () => {
            const container = btn.closest('.ratio-grid');
            if (container) {
                container.querySelectorAll('.ratio-item').forEach(r => r.classList.remove('active'));
                btn.classList.add('active');
            }

            // 同步更新上级下拉菜单标签
            const parentDropdown = btn.closest('.control-dropdown');
            if (parentDropdown) {
                const labelSpan = parentDropdown.querySelector('span:first-child');
                if (labelSpan) {
                    labelSpan.textContent = btn.querySelector('span')?.textContent || btn.textContent;
                }
            }
        });
    });

    // 分辨率按钮（720P, 1080P, 2K）
    document.querySelectorAll('.resolution-btn').forEach(btn => {
        btn.addEventListener('click', () => {
            const container = btn.closest('.resolution-options');
            if (container) {
                container.querySelectorAll('.resolution-btn').forEach(r => r.classList.remove('active'));
                btn.classList.add('active');
            }

            // 更新分辨率标签显示
            const parentDropdown = btn.closest('.control-dropdown');
            if (parentDropdown) {
                const resLabel = parentDropdown.querySelector('.res-label');
                if (resLabel) resLabel.textContent = btn.textContent.trim();
            }
        });
    });
}
```

### 1.6.3 数量选择器

> **生成数量选择**（1张 / 2张 / 4张）的 active 状态 + 文本更新

```javascript
/**
 * 初始化数量选择器（1张 / 2张 / 4张）
 */
function initQuantitySelector() {
    const qtyItems = document.querySelectorAll('.qty-item');

    qtyItems.forEach(item => {
        item.addEventListener('click', () => {
            const container = item.closest('.quantity-dropdown');
            if (container) {
                container.querySelectorAll('.qty-item').forEach(q => q.classList.remove('active'));
                item.classList.add('active');
            }

            // 更新显示为 "X / 张"
            const parentDrop = item.closest('.control-dropdown');
            if (parentDrop) {
                const targetSpan = parentDrop.querySelector('span:not(.qty-item)');
                if (targetSpan) targetSpan.textContent = item.textContent + ' / 张';
            }
        });
    });
}
```

### 1.6.4 场景模式切换 ⚠️ 重要

> **平台骨架功能**：图片 ↔ 视频 ↔ 数字人 三种模式的切换，控制面板显隐 + 自动刷新模型列表

```javascript
/**
 * 初始化场景类型切换（图片 / 视频 / 数字人）
 * - 切换时隐藏/显示对应的面板
 * - 自动刷新模型列表以匹配新场景
 */
function initSceneSwitch() {
    const sceneItems = document.querySelectorAll('.dropdown-item[data-scene]');

    sceneItems.forEach(item => {
        item.addEventListener('click', (e) => {
            e.stopPropagation();
            const scene = item.getAttribute('data-scene');

            // 隐藏所有场景面板
            document.querySelectorAll('.scene-controls').forEach(ctrl => ctrl.style.display = 'none');
            document.querySelectorAll('.digital-human-inputs').forEach(extra => extra.style.display = 'none');

            // 显示当前选中的场景面板
            if (scene === 'video') {
                document.querySelector('.video-scene')?.style.setProperty('display', 'flex');
            } else if (scene === 'digital-human') {
                document.querySelector('.digital-human-scene')?.style.setProperty('display', 'flex');
                document.querySelector('.digital-human-inputs')?.style.setProperty('display', 'block');
            } else {
                document.querySelector('.image-scene')?.style.setProperty('display', 'flex');
            }

            // 更新 active 状态
            sceneItems.forEach(si => si.classList.remove('active'));
            document.querySelectorAll(`.dropdown-item[data-scene="${scene}"]`)
                .forEach(si => si.classList.add('active'));

            // 同步更新各面板的下拉菜单标签
            document.querySelectorAll('.scene-controls').forEach(ctrl => {
                const firstDropdown = ctrl.querySelector('.control-dropdown');
                if (firstDropdown) {
                    const labelSpan = firstDropdown.querySelector('span:first-child');
                    if (labelSpan && !labelSpan.querySelector('i')) {
                        const activeItem = ctrl.querySelector(`.dropdown-item[data-scene="${scene}"]`);
                        if (activeItem) labelSpan.textContent = activeItem.textContent.trim();
                    }
                }
            });

            // 关闭下拉菜单
            item.closest('.dropdown-menu')?.style.setProperty('display', 'none');

            // 🔄 关键：自动刷新模型列表以匹配新场景
            refreshModelsWithParams();
        });
    });
}
```

---

## 1.7 主初始化入口 ⚠️ 必须迁移

> **页面加载时的完整启动流程**，新平台必须按此顺序调用

```javascript
/**
 * 主初始化函数 - 页面加载完成后按顺序执行所有模块初始化
 *
 * 初始化顺序说明：
 * 1. 基础UI组件（登录、导航、搜索）
 * 2. 文件上传功能
 * 3. 下拉菜单、按钮等交互控件
 * 4. 场景切换（必须在模型初始化之前）
 * 5. 发送按钮绑定
 * 6. 模型列表从后端拉取并渲染（最后，依赖前面的DOM就绪）
 */
function initAll() {
    // ===== 第一层：基础框架 =====
    initLoginTabs();          // 登录页Tab切换
    initLoginForm();          // 登录表单提交
    initAssetFilters();       // 资源筛选器（社区页面）
    initAssetDelete();        // 资源删除功能
    initAdminNav();           // 管理后台导航
    initCommunityInteraction(); // 社区互动功能
    initPagination();         // 分页组件
    initSearch();             // 搜索功能
    initBatchActions();       // 批量操作
    initDynamicChat();        // 动态聊天窗口

    // ===== 第二层：核心交互 =====
    initFileUpload();         // 文件上传（本地上传+历史文件）
    initDropdowns();          // 所有下拉菜单通用交互
    initRatioButtons();       // 比例+分辨率按钮切换
    initResolutionSelector(); // 分辨率选择器（预留扩展）
    initQuantitySelector();   // 数量选择器（1/2/4张）
    initSceneSwitch();        // 场景切换（图片↔视频↔数字人）

    // ===== 第三层：业务逻辑 =====
    initSendButton();         // 发送按钮与暂停功能
    initModels();             // 🆕 从后端拉取模型列表并渲染（最后执行）
}

// 页面加载时启动：
// document.addEventListener('DOMContentLoaded', initAll);
// 或在 <body> 底部直接调用：
// initAll();
```

---

## 2. 后端模型拉取与渲染 ⭐

> **这是平台最核心的功能之一**：页面加载时从后端拉取模型列表，按厂商分组渲染到下拉选择器。

### 2.1 完整流程图

```
页面加载 → initModels()
    │
    ├─ testApiConnection()          // 测试后端连通性
    │   └─ GET /models (简单请求)
    │
    ├─ fetchModels()                // 获取完整模型列表
    │   └─ GET /models
    │       └─ 返回 { image_models, video_models, voices, summary }
    │           └─ 存入 globalModels 全局变量
    │
    ├─ renderImageModels()          // 渲染图片模型下拉列表
    │   └─ 按厂商(vendor)分组显示
    │       └─ vendor_a: 腾讯云 VOD
    │       └─ vendor_b: Token Switch
    │
    ├─ renderVideoModels()          // 渲染视频模型下拉列表
    │   └─ 同上，按厂商分组
    │
    └─ bindRatioResolutionEvents()  // 绑定参数变化事件
        └─ 切换比例/分辨率 → 自动刷新模型列表
```

### 2.2 API 连通性测试

```javascript
/**
 * 测试 API 连通性（轻量级）
 * @returns {Promise<boolean>} 是否连通
 */
async function testApiConnection() {
    try {
        const response = await fetch(`${API_CONFIG.BASE_URL}/models`, {
            method: 'GET',
            headers: {
                'Content-Type': 'application/json',
                'Authorization': `Bearer ${localStorage.getItem('token') || ''}`
            }
        });
        if (!response.ok) return false;
        const data = await response.json();
        console.log('✅ API 连通性测试成功:', data);
        return data.code === 200;
    } catch (error) {
        console.error('❌ API 连通性测试失败:', error);
        return false;
    }
}
```

### 2.3 获取模型列表（核心函数）

```javascript
/**
 * 从后端获取可用模型列表
 * @returns {Promise<Object>} { image_models, video_models, voices }
 */
async function fetchModels() {
    try {
        const response = await fetch(`${API_CONFIG.BASE_URL}/models`, {
            method: 'GET',
            headers: {
                'Content-Type': 'application/json',
                'Authorization': `Bearer ${localStorage.getItem('token') || ''}`
            }
        });

        if (!response.ok) throw new Error(`获取模型列表失败: ${response.status}`);

        const data = await response.json();
        if (data.code !== 200) throw new Error(data.message || '获取模型列表失败');

        // ✅ 保存到全局状态
        globalModels = data.data || { image_models: [], video_models: [], voices: [] };
        console.log('✅ 模型列表获取成功:', globalModels);

        // ✅ 打印统计信息
        if (data.data?.summary) {
            const s = data.data.summary;
            console.log('📊 模型统计:',
                `图片=${s.total_image_models}, 视频=${s.total_video_models}, 音色=${s.total_voices}`,
                '| 厂商:', s.vendors?.join(', '));
        }

        return globalModels;
    } catch (error) {
        console.error('❌ 获取模型列表失败:', error);
        showToast('获取模型列表失败，请检查后端服务', 'error');
        return { image_models: [], video_models: [], voices: [] };
    }
}
```

### 2.4 带参数获取模型（按比例/分辨率筛选）

```javascript
/**
 * 根据当前选择的参数（场景类型、比例、分辨率）获取模型列表
 * @param {Object} params { sceneType, ratio, resolution }
 */
async function fetchModelsWithParams(params) {
    try {
        const queryParams = new URLSearchParams({
            type: params.sceneType,
            ratio: params.ratio,
            resolution: params.resolution
        }).toString();

        const response = await fetch(`${API_CONFIG.BASE_URL}/models?${queryParams}`, {
            method: 'GET',
            headers: {
                'Content-Type': 'application/json',
                'Authorization': `Bearer ${localStorage.getItem('token') || ''}`
            }
        });

        if (!response.ok) throw new Error(`获取模型列表失败: ${response.status}`);
        const data = await response.json();
        if (data.code !== 200) throw new Error(data.message || '获取模型列表失败');

        globalModels = data.data || { image_models: [], video_models: [], voices: [] };
        console.log('✅ 模型列表获取成功（带参数）:', globalModels, '参数:', params);
        return globalModels;
    } catch (error) {
        console.error('❌ 获取模型列表失败:', error);
        return { image_models: [], video_models: [], voices: [] };
    }
}
```

### 2.5 页面初始化入口

```javascript
/**
 * 初始化模型列表（页面加载时调用）
 * 流程：测试连通 → 获取模型 → 渲染UI → 绑定事件
 */
async function initModels() {
    console.log('🚀 开始初始化模型列表...');

    // 第一步：测试 API 连通性
    const isConnected = await testApiConnection();

    if (!isConnected) {
        // ⚠️ 后端不可用 → 使用硬编码默认模型
        console.warn('⚠️ API 未连通，将使用默认硬编码模型');
        showToast('无法连接到后端服务，使用默认模型', 'warning');
        renderImageModels([]);     // 空数组触发硬编码回退
        renderVideoModels([]);
    } else {
        // ✅ 后端可用 → 拉取真实模型数据
        const models = await fetchModels();

        if (models.image_models && models.image_models.length > 0) {
            renderImageModels(models.image_models);
        } else {
            renderImageModels([]);  // 触发硬编码回退
        }
        if (models.video_models && models.video_models.length > 0) {
            renderVideoModels(models.video_models);
        } else {
            renderVideoModels([]);
        }
    }

    // 绑定比例/分辨率切换事件（自动刷新模型）
    bindRatioResolutionEvents();

    console.log('✅ 模型列表初始化完成');
}

// 在 DOMContentLoaded 或页面底部调用：
// initModels();
```

### 2.6 图片模型渲染（按厂商分组）

```javascript
function renderImageModels(models) {
    const imageModelDropdown = document.querySelector('#imageModelDropdown .dropdown-menu');
    if (!imageModelDropdown) {
        console.warn('未找到图片模型下拉菜单容器');
        return;
    }

    // 清空现有内容
    const header = imageModelDropdown.querySelector('.model-panel-header');
    imageModelDropdown.innerHTML = '';
    if (header) {
        header.textContent = '请选择模型';
        imageModelDropdown.appendChild(header);
    }

    // ===== 添加默认选项 =====
    const defaultOption = document.createElement('div');
    defaultOption.className = 'model-option active';
    defaultOption.setAttribute('data-model-id', '');
    defaultOption.innerHTML = `
        <div class="model-icon"><i class="fas fa-cube"></i></div>
        <div class="model-info">
            <div class="model-name">-- 请选择模型 --</div>
            <div class="model-desc">根据比例和分辨率自动推荐</div>
        </div>
        <div class="model-check"><i class="fas fa-check"></i></div>
    `;
    imageModelDropdown.appendChild(defaultOption);

    // 更新触发按钮文本
    const triggerBtn = document.querySelector('#imageModelDropdown > span');
    if (triggerBtn) triggerBtn.textContent = '-- 请选择 --';

    // ===== 无数据时的硬编码回退 =====
    if (!models || models.length === 0) {
        console.warn('没有获取到图片模型，使用默认硬编码');
        models = [
            { id: 'image_5.0_lite', name: '图片5.0 Lite', description: '指令响应更精准', is_new: true, vendor: 'vendor_a', vendor_name: '腾讯云 VOD' },
            { id: 'image_5.0', name: '图片5.0', description: '全能王者，支持文生图、图生图等全能力', is_new: true, is_vip: true, free_trial: true, vendor: 'vendor_a', vendor_name: '腾讯云 VOD' },
            { id: 'hunyuan_1_5', name: 'Hunyuan 1.5', description: '混元大模型，支持中文理解', vendor: 'vendor_a', vendor_name: '腾讯云 VOD' },
            // ... 更多硬编码模型
        ];
    }

    // ===== 按 vendor 分组 =====
    const vendorGroups = {};
    models.forEach(m => {
        const v = m.vendor || 'unknown';
        if (!vendorGroups[v]) vendorGroups[v] = [];
        vendorGroups[v].push(m);
    });

    Object.entries(vendorGroups).forEach(([vendorId, groupModels]) => {
        const groupName = groupModels[0]?.vendor_name || vendorId;

        // 厂商标题头
        const groupHeader = document.createElement('div');
        groupHeader.className = 'model-vendor-group';
        groupHeader.innerHTML = `<span class="vendor-label">${groupName}</span>`;
        imageModelDropdown.appendChild(groupHeader);

        // 渲染该厂商下的每个模型
        groupModels.forEach(model => {
            const modelOption = document.createElement('div');
            modelOption.className = 'model-option';
            modelOption.setAttribute('data-model-id', model.id);      // ← 用于 vendor_b 提交
            modelOption.setAttribute('data-vendor', model.vendor || ''); // ← 用于判断提交规则

            // 徽章标签
            const badgeHtml = model.is_new ? '<span class="model-badge">New</span>' : '';
            const vipBadge = model.is_vip ? '<span class="model-badge-diamond">&#10022;</span>' : '';
            const freeBadge = model.free_trial ? '<span class="model-badge-special">限免1次</span>' : '';

            modelOption.innerHTML = `
                <div class="model-icon"><i class="fas fa-paper-plane"></i></div>
                <div class="model-info">
                    <div class="model-name">${model.name}${badgeHtml}${vipBadge}${freeBadge}</div>
                    <div class="model-desc">${model.description || ''}</div>
                </div>
                <div class="model-check"><i class="fas fa-check"></i></div>
            `;
            imageModelDropdown.appendChild(modelOption);
        });
    });

    // 绑定点击事件
    bindModelOptionEvents();

    console.log(`✅ 图片模型列表渲染完成，共 ${models.length} 个模型`,
        '厂商分组:', Object.keys(vendorGroups));
}
```

### 2.7 视频模型渲染（同上结构）

```javascript
function renderVideoModels(models) {
    const videoModelDropdown = document.querySelector('#videoModelDropdown .dropdown-menu');
    if (!videoModelDropdown) return;

    // ... 结构与 renderImageModels 完全相同 ...

    // 硬编码回退示例：
    if (!models || models.length === 0) {
        models = [
            { id: 'seedance_2.0_fast_vip', name: 'Seedance 2.0 Fast VIP', description: '极速推理', is_new: true, is_vip: true, vendor: 'vendor_a', vendor_name: '腾讯云 VOD' },
            { id: 'seedance_2.0_fast', name: 'Seedance 2.0 Fast', description: '高性价比', is_new: true, vendor: 'vendor_a', vendor_name: '腾讯云 VOD' },
            { id: 'happyhorse-1.0-video-edit-720p', name: 'HappyHorse Video Edit 720p', description: '视频编辑', vendor: 'vendor_b', vendor_name: 'Token Switch' },
            // ...
        ];
    }

    // 同样按 vendor 分组渲染...
}
```

### 2.8 模型选项点击事件绑定

```javascript
function bindModelOptionEvents() {
    document.querySelectorAll('.model-option').forEach(option => {
        // 移除旧事件（通过克隆节点）
        const newOption = option.cloneNode(true);
        option.parentNode.replaceChild(newOption, option);

        newOption.addEventListener('click', (e) => {
            e.stopPropagation();
            const menu = newOption.closest('.dropdown-menu');
            if (!menu) return;

            // 移除其他 active 状态
            menu.querySelectorAll('.model-option').forEach(opt => {
                opt.classList.remove('active');
                const check = opt.querySelector('.model-check');
                if (check) check.remove();
            });

            // 设置当前为 active
            newOption.classList.add('active');
            if (!newOption.querySelector('.model-check')) {
                const checkDiv = document.createElement('div');
                checkDiv.className = 'model-check';
                checkDiv.innerHTML = '<i class="fas fa-check"></i>';
                newOption.appendChild(checkDiv);
            }

            // 更新按钮显示文本（提取纯名称，去掉 badge）
            const parentDrop = menu.closest('.control-dropdown');
            const labelSpan = parentDrop?.querySelector('span:first-child');
            const nameEl = newOption.querySelector('.model-name');
            if (labelSpan && nameEl) {
                const modelName = nameEl.childNodes[0]?.textContent?.trim()
                    || nameEl.textContent.trim();
                labelSpan.textContent = modelName;
            }

            // 关闭下拉菜单
            menu.style.display = 'none';

            console.log('🎯 已选择模型:', {
                id: newOption.getAttribute('data-model-id'),
                vendor: newOption.getAttribute('data-vendor')
            });
        });
    });
}
```

### 2.9 参数变化自动刷新模型

```javascript
/**
 * 当用户切换比例或分辨率时，重新拉取并渲染模型列表
 */
async function refreshModelsWithParams() {
    console.log('🔄 根据参数刷新模型列表...');
    const params = getCurrentParams();  // 获取当前场景类型、比例、分辨率
    const models = await fetchModelsWithParams(params);

    if (params.sceneType === 'image') {
        renderImageModels(models.image_models);
    } else if (params.sceneType === 'video') {
        renderVideoModels(models.video_models);
    }
}

/**
 * 绑定比例和分辨率选择器的 change 事件
 */
function bindRatioResolutionEvents() {
    // 图片场景 - 比例选择
    document.querySelectorAll('#imageRatioResDropdown .ratio-item').forEach(item => {
        item.addEventListener('click', async () => {
            setTimeout(async () => await refreshModelsWithParams(), 100);
        });
    });

    // 图片场景 - 分辨率选择
    document.querySelectorAll('#imageRatioResDropdown .resolution-btn').forEach(btn => {
        btn.addEventListener('click', async () => {
            setTimeout(async () => await refreshModelsWithParams(), 100);
        });
    });

    // 视频场景 - 同上绑定...
}
```

### 2.10 后端返回的模型数据结构

```typescript
// 单个模型对象
interface ModelItem {
    id: string;           // 内部ID（vendor_b 提交用这个）
    name: string;         // 显示名（vendor_a 提交用这个）
    description: string;  // 模型描述
    vendor: 'vendor_a' | 'vendor_b' | string;  // 厂商标识
    vendor_name: string;  // 厂商中文名
    is_new: boolean;      // 是否新品
    is_vip: boolean;      // 是否会员专属
    free_trial?: boolean; // 是否有限免
}

// API 响应结构
interface ModelsResponse {
    code: number;
    message: string;
    data: {
        summary?: {
            total_image_models: number;
            total_video_models: number;
            total_voices: number;
            vendors: string[];
        };
        image_models: ModelItem[];
        video_models: ModelItem[];
        voices: VoiceItem[];
    };
}
```

---

## 3. 用户文件上传功能

### 3.1 核心函数：initFileUpload()

```javascript
function initFileUpload() {
    const uploadBtn = document.getElementById('uploadBtn');
    const uploadMenu = document.getElementById('uploadMenu');
    const localFileInput = document.getElementById('localFileInput');
    const previewContainer = document.getElementById('uploadedFilesPreview');
    
    // 历史文件弹窗相关元素
    const historyModalOverlay = document.getElementById('historyModalOverlay');
    const historyModalClose = document.getElementById('historyModalClose');
    const historyModalBody = document.getElementById('historyModalBody');
    const historyConfirmBtn = document.getElementById('historyConfirmBtn');

    if (!uploadBtn || !previewContainer) return;

    // 上传按钮点击 → 显示菜单（本地上传 / 历史文件）
    uploadBtn.addEventListener('click', (e) => {
        e.stopPropagation();
        uploadMenu.classList.toggle('show');
    });

    // 点击其他地方关闭菜单
    document.addEventListener('click', () => {
        uploadMenu.classList.remove('show');
    });

    // 本地上传 → 触发 file input
    uploadMenu.querySelector('[data-action="local"]')?.addEventListener('click', (e) => {
        e.stopPropagation();
        uploadMenu.classList.remove('show');
        localFileInput.click();
    });

    // 历史文件 → 打开弹窗
    uploadMenu.querySelector('[data-action="history"]')?.addEventListener('click', (e) => {
        e.stopPropagation();
        uploadMenu.classList.remove('show');
        openHistoryModal();
    });
}
```

### 3.2 本地上传处理

```javascript
// 监听 file input 变化，读取文件为 Base64
localFileInput.addEventListener('change', () => {
    const files = Array.from(localFileInput.files);
    files.forEach(file => {
        const reader = new FileReader();
        reader.onload = () => {
            const fileType = file.type.startsWith('image/') ? 'image' :
                file.type.startsWith('video/') ? 'video' :
                file.type.startsWith('audio/') ? 'audio' : 'other';
            uploadedFiles.push({
                id: 'local_' + Date.now() + '_' + Math.random().toString(36).substr(2, 6),
                type: fileType,
                url: reader.result,           // Base64 data URL
                name: file.name,
                purpose: 'reference'
            });
            renderFilePreviews();
        };
        reader.readAsDataURL(file);
    });
    localFileInput.value = '';  // 重置，允许重复选同一文件
});
```

### 3.3 历史文件功能

```javascript
// 从 localStorage 获取历史文件列表
function getHistoryFiles() {
    try {
        const raw = localStorage.getItem(HISTORY_STORAGE_KEY);
        return raw ? JSON.parse(raw) : [];
    } catch (e) { return []; }
}

// 添加文件到历史记录（最多50条）
function addToHistory(file) {
    const history = getHistoryFiles();
    history.unshift({
        id: 'hist_' + Date.now() + '_' + Math.random().toString(36).substr(2, 6),
        type: file.type || 'image',
        url: file.url || file,
        name: file.name || ''
    });
    if (history.length > 50) history.length = 50;
    localStorage.setItem(HISTORY_STORAGE_KEY, JSON.stringify(history));
}

// 打开历史文件选择弹窗
function openHistoryModal() {
    selectedHistoryFiles = [];
    renderHistoryFiles();
    historyModalOverlay.style.display = 'flex';
}

// 渲染历史文件列表（支持多选）
function renderHistoryFiles() {
    const history = getHistoryFiles();
    if (history.length === 0) {
        historyModalBody.innerHTML = '<div class="history-empty">暂无历史生成文件</div>';
        return;
    }
    historyModalBody.innerHTML = history.map(file => {
        const isSelected = selectedHistoryFiles.some(f => f.id === file.id);
        const mediaContent = file.type === 'image'
            ? `<img src="${file.url}" alt="${file.name || ''}">`
            : `<video src="${file.url}" muted></video>`;
        return `<div class="history-file-item${isSelected ? ' selected' : ''}" data-file-id="${file.id}">
            ${mediaContent}
        </div>`;
    }).join('');
}

// 确认选择历史文件
historyConfirmBtn.addEventListener('click', () => {
    selectedHistoryFiles.forEach(file => {
        if (!uploadedFiles.some(f => f.id === file.id)) {
            uploadedFiles.push({ ...file, purpose: 'reference' });
        }
    });
    renderFilePreviews();
    historyModalOverlay.style.display = 'none';
});
```

### 3.4 文件预览渲染

```javascript
function renderFilePreviews() {
    const previewContainer = document.getElementById('uploadedFilesPreview');
    if (uploadedFiles.length === 0) {
        previewContainer.style.display = 'none';
        previewContainer.innerHTML = '';
        return;
    }
    previewContainer.style.display = 'flex';
    previewContainer.innerHTML = uploadedFiles.map((file, index) => {
        let content;
        if (file.type === 'image') {
            content = `<img src="${file.url}" alt="${file.name || ''}">`;
        } else if (file.type === 'video') {
            content = `<video src="${file.url}" muted></video>`;
        } else if (file.type === 'audio') {
            content = `<div class="file-type-icon"><i class="fas fa-volume-up"></i></div>`;
        } else {
            content = `<div class="file-type-icon"><i class="fas fa-file"></i></div>`;
        }
        return `<div class="file-preview-item" data-file-index="${index}">
            ${content}
            <button class="file-preview-delete" data-delete-index="${index}">&times;</button>
        </div>`;
    }).join('');

    // 删除按钮事件绑定
    previewContainer.querySelectorAll('.file-preview-delete').forEach(btn => {
        btn.addEventListener('click', (e) => {
            e.stopPropagation();
            const idx = parseInt(btn.getAttribute('data-delete-index'), 10);
            uploadedFiles.splice(idx, 1);
            renderFilePreviews();
        });
    });
}
```

### 3.5 文件数据结构

```javascript
// uploadedFiles 数组中每个对象的结构：
{
    id: 'local_1704000000_abc123',   // 唯一标识
    type: 'image' | 'video' | 'audio' | 'other',
    url: 'data:image/png;base64,...',  // Base64 data URL 或 blob URL
    name: 'photo.png',                 // 原始文件名
    purpose: 'reference',              // 用途标识
    object_id: 'image_1'               // 多文件时的引用ID（可选）
}
```

---

## 4. 对话交互功能

### 4.1 添加用户消息

```javascript
function addUserMessage(text, files) {
    const msgDiv = document.createElement('div');
    msgDiv.className = 'message message-user';
    let fileHtml = '';
    if (files && files.length > 0) {
        const previews = files.map(file => {
            if (file.type === 'image') {
                return `<img src="${file.url}" alt="${escapeHtml(file.name || '')}"
                    style="max-width:200px;max-height:200px;border-radius:8px;margin:4px;cursor:pointer;"
                    onclick="window.open('${file.url}','_blank')">`;
            } else if (file.type === 'video') {
                return `<video src="${file.url}" controls
                    style="max-width:280px;max-height:200px;border-radius:8px;margin:4px;"></video>`;
            }
            return `<div>📄 ${escapeHtml(file.name || file.type)}</div>`;
        }).join('');
        fileHtml = `<div class="message-files" style="margin-top:6px;">${previews}</div>`;
    }
    msgDiv.innerHTML = `<div class="message-content">
        ${text ? '<div class="message-text">' + escapeHtml(text) + '</div>' : ''}${fileHtml}
    </div>`;
    chatMessages.appendChild(msgDiv);
    scrollToBottom();
}
```

### 4.2 加载中消息

```javascript
function addLoadingMessage(modelName) {
    const loadingDiv = document.createElement('div');
    loadingDiv.className = 'message message-bot';
    loadingDiv.innerHTML = `
        <div class="message-content">
            <div class="message-text">模型 ${escapeHtml(modelName)} 正在生成，请稍候...</div>
            <div class="message-loading"><i class="fas fa-spinner fa-spin"></i></div>
        </div>
    `;
    chatMessages.appendChild(loadingDiv);
    scrollToBottom();
    return loadingDiv;
}
```

### 4.3 结果渲染（图片/视频/文字/错误）

```javascript
function updateLoadingToResult(loadingElem, type, data) {
    const feedbackText = data.feedback || '大模型已返回结果。';

    if (type === 'image') {
        const imagesHtml = (data.images || []).map((url, i) => {
            const isBlob = url.startsWith('blob:');
            const dlName = isBlob ? `generated_image_${i + 1}.png` : '';
            return `
            <div class="image-item" style="position:relative;display:inline-block;margin:4px;">
                <img src="${url}" alt="生成的图片${i+1}"
                    style="max-width:280px;border-radius:8px;cursor:pointer;"
                    onclick="window.open('${url}','_blank')">
                <a href="${url}" download="${dlName}" target="_blank"
                    style="position:absolute;bottom:6px;right:6px;background:rgba(0,0,0,0.7);color:#fff;
                           padding:4px 10px;border-radius:4px;font-size:12px;text-decoration:none;">
                    <i class="fas fa-download"></i> 下载
                </a>
            </div>`;}).join('');
        loadingElem.innerHTML = `
            <div class="message-content">
                <div class="message-text">${escapeHtml(feedbackText)}</div>
                <div class="message-images">${imagesHtml}</div>
            </div>`;

    } else if (type === 'video') {
        loadingElem.innerHTML = `
            <div class="message-content">
                <div class="message-text">${escapeHtml(feedbackText)}</div>
                <div class="message-video" style="position:relative;">
                    <video controls src="${data.videoUrl}" style="max-width:100%;border-radius:8px;"></video>
                    <a href="${data.videoUrl}" download target="_blank"
                        style="display:inline-block;margin-top:6px;background:#3182ce;color:#fff;
                               padding:6px 16px;border-radius:6px;font-size:13px;text-decoration:none;">
                        <i class="fas fa-download"></i> 下载视频
                    </a>
                </div>
            </div>`;

    } else if (type === 'text') {
        loadingElem.innerHTML = `
            <div class="message-content">
                <div class="message-text">${escapeHtml(feedbackText)}</div>
            </div>`;

    } else {  // error
        loadingElem.innerHTML = `
            <div class="message-content">
                <div class="message-text" style="color:#e53e3e;">
                    ❌ ${escapeHtml(data.message || '生成失败')}
                </div>
            </div>`;
    }
    scrollToBottom();
}
```

### 4.4 辅助函数

```javascript
function scrollToBottom() {
    chatMessages.scrollTop = chatMessages.scrollHeight;
}

function escapeHtml(str) {
    return str.replace(/[&<>]/g, function(m) {
        if (m === '&') return '&amp;';
        if (m === '<') return '&lt;';
        if (m === '>') return '&gt;';
        return m;
    });
}
```

---

## 5. 发送按钮与暂停功能

### 5.1 初始化发送按钮

```javascript
function initSendButton() {
    const sendBtn = document.querySelector('.send-btn');
    const chatInput = document.querySelector('.chat-input');
    const chatMessages = document.querySelector('.chat-messages');

    let currentAbortController = null;
    let isSending = false;
    let currentTaskId = null;

    // 按钮状态切换：发送 ↔ 暂停
    function setButtonState(sending) {
        isSending = sending;
        if (sending) {
            sendBtn.classList.add('sending');
            sendBtn.querySelector('i').className = 'fas fa-stop';       // 暂停图标
        } else {
            sendBtn.classList.remove('sending');
            sendBtn.querySelector('i').className = 'fas fa-paper-plane'; // 发送图标
            currentAbortController = null;
            currentTaskId = null;
        }
    }

    // 点击事件：发送 or 暂停
    sendBtn.addEventListener('click', handleSendClick);

    // 回车键触发发送
    chatInput.addEventListener('keypress', handleKeyPress);

    function handleKeyPress(e) {
        if (e.key === 'Enter' && !e.shiftKey) {
            e.preventDefault();
            handleSendClick();
        }
    }

    function handleSendClick() {
        if (isSending) {
            handleStop();     // 正在发送 → 暂停
        } else {
            handleSend();     // 空闲 → 发送
        }
    }
}
```

### 5.2 暂停/取消逻辑

```javascript
function handleStop() {
    if (currentAbortController) {
        currentAbortController.abort();      // 中止 fetch 请求
    }
    if (currentTaskId) {
        cancelTask(currentTaskId);            // 调用后端取消接口
    }
    setButtonState(false);
    showToast('已中止生成', 'info');
}

async function cancelTask(taskId) {
    try {
        const token = localStorage.getItem('token');
        const headers = { 'Content-Type': 'application/json' };
        if (token) headers['Authorization'] = `Bearer ${token}`;
        await fetch(`${API_CONFIG.BASE_URL}/tasks/${taskId}/cancel`, {
            method: 'POST',
            headers
        });
    } catch (e) {
        console.warn('取消任务请求失败:', e);
    }
}
```

### 5.3 发送主流程

```javascript
async function handleSend() {
    const prompt = chatInput.value.trim();
    if (!prompt && uploadedFiles.length === 0) {
        showToast('请输入描述内容或上传参考图', 'warning');
        return;
    }

    const modelName = getCurrentModelName();
    const currentFiles = [...uploadedFiles];

    // 1. 显示用户消息
    addUserMessage(prompt, currentFiles);

    // 2. 清空输入和文件预览
    chatInput.value = '';
    uploadedFiles = [];
    renderFilePreviewsAfterSend();

    // 3. 大文件提示
    if (currentFiles.length > 0) {
        const videoFiles = currentFiles.filter(f => f.type === 'video');
        const totalSizeMB = videoFiles.reduce((sum, f) =>
            sum + (f.url?.length || 0) * 0.75 / 1024 / 1024, 0);
        if (totalSizeMB > 50) {
            showToast(`视频文件较大(${totalSizeMB.toFixed(0)}MB)，上传可能需要较长时间`, 'warning');
        }
    }

    // 4. 显示加载状态
    const loadingMsg = addLoadingMessage(modelName);
    currentAbortController = new AbortController();
    setButtonState(true);

    try {
        // 5. 构建请求并发送
        const sceneType = getActiveSceneType();
        const requestBody = buildGenerateRequest(prompt || '(无文字)', sceneType, currentFiles);
        const response = await postJson(requestBody.endpoint, requestBody.body, currentAbortController.signal);

        // 6. 处理响应
        if (response.code !== 200) throw new Error(response.message || '生成接口返回异常');

        const rawData = response.data || {};
        const extractedResult = extractResultFromData(rawData);

        if (extractedResult) {
            const taskResult = {
                task_id: rawData.task_id || 'sync_' + Date.now(),
                status: rawData.status || 'completed',
                result: extractedResult
            };
            await renderTaskResult(loadingMsg, taskResult, sceneType);
        } else {
            throw new Error('后端未返回有效数据');
        }

    } catch (error) {
        if (error.name === 'AbortError') {
            updateLoadingToResult(loadingMsg, 'text', { feedback: '生成已中止' });
        } else {
            updateLoadingToResult(loadingMsg, 'error', {
                message: error.message || '生成失败，请稍后重试'
            });
        }
    } finally {
        setButtonState(false);
    }
}
```

---

## 6. 请求构建功能

### 6.1 HTTP 请求封装

```javascript
async function postJson(path, body, signal = null) {
    const token = localStorage.getItem('token');
    const headers = { 'Content-Type': 'application/json' };
    if (token) headers['Authorization'] = `Bearer ${token}`;

    const url = path.startsWith('http') ? path : `${API_CONFIG.BASE_URL}${path}`;
    const bodyStr = JSON.stringify(body);
    const bodySizeKB = new Blob([bodyStr]).size / 1024;

    // 动态超时：<1MB=120s, 1-10MB=300s, >10MB=600s
    const timeoutMs = bodySizeKB > 10 * 1024 ? 600000 :
                      (bodySizeKB > 1024 ? 300000 : 120000);

    const timeoutController = new AbortController();
    const timeoutId = setTimeout(() => timeoutController.abort(), timeoutMs);

    let finalSignal = signal;
    if (signal) signal.addEventListener('abort', () => timeoutController.abort());
    else finalSignal = timeoutController.signal;

    try {
        const response = await fetch(url, {
            method: 'POST', headers, body: bodyStr, signal: finalSignal
        });
        clearTimeout(timeoutId);
        if (!response.ok) throw new Error(`网络请求失败: ${response.status}`);
        return response.json();
    } catch (err) {
        clearTimeout(timeoutId);
        if (err.name === 'AbortError')
            throw new Error(`请求超时 (${timeoutMs / 1000}秒)，文件可能过大`);
        throw err;
    }
}
```

### 6.2 生成请求体构建

```javascript
function buildGenerateRequest(prompt, sceneType, files) {
    const modelName = getSelectedModelId();
    const vendor = window.__selectedModelVendor || '';

    // ⭐ 关键：按 vendor 决定 model 字段值
    const submitModel = (vendor === 'vendor_b' && window.__selectedModelId)
        ? window.__selectedModelId : modelName;

    const ratio = getSelectedRatio();
    const resolution = getSelectedResolution();

    // 构建 input_files 数组
    const inputFiles = (files && files.length > 0)
        ? files.map((file, index) => ({
              type: file.type,
              url: file.url,
              purpose: file.purpose || 'reference',
              object_id: file.object_id || `${file.type}_${index + 1}`
          }))
        : buildInputFiles();

    // ===== 场景1：视频生成 =====
    if (sceneType === 'video') {
        let feature, finalPrompt = prompt;
        if (inputFiles && inputFiles.length > 0) {
            feature = inputFiles.length >= 2 ? 'multi_reference' : 'global_reference';
            if (inputFiles.length >= 2) {
                const refs = inputFiles.map(f => `<<<${f.object_id}>>>`).join(' 和 ');
                finalPrompt = `${prompt} ${refs}`;
            }
        } else {
            feature = mapRefModeToFeature(getSelectedRefMode());
        }
        const params = {
            resolution: resolution || '1080P',
            duration: getSelectedDuration(),
            ratio: ratio || '16:9'
        };

        return {
            endpoint: '/generate?sync=true',
            body: {
                output_type: 'video',
                model: submitModel || 'kling_3.0',
                vendor: vendor || undefined,
                feature: feature,
                parameters: params,
                prompt: finalPrompt,
                input_files: inputFiles
            }
        };
    }

    // ===== 场景2：数字人 =====
    if (sceneType === 'digital-human') {
        return {
            endpoint: '/generate?sync=true',
            body: {
                output_type: 'digital_human',
                model: submitModel || 'kling_2.6',
                vendor: vendor || undefined,
                feature: 'digital_human',
                parameters: {
                    voice_id: getSelectedVoiceId(),
                    action_description: document.querySelector('#actionDesc')?.value.trim() || ''
                },
                prompt: document.querySelector('#speechContent')?.value.trim() || prompt,
                input_files: inputFiles
            }
        };
    }

    // ===== 场景3：图片生成（默认）=====
    return {
        endpoint: '/generate?sync=true',
        body: {
            output_type: 'image',
            model: submitModel || 'image_5.0_lite',
            vendor: vendor || undefined,
            feature: (inputFiles && inputFiles.length > 0)
                ? 'image_reference' : getSelectedImageFeature(),
            parameters: {
                resolution: resolution || '1080P',
                ratio: ratio || '1:1',
                count: getSelectedCount()
            },
            prompt: prompt,
            input_files: inputFiles
        }
    };
}
```

---

## 7. 结果渲染功能

### 7.1 Base64 图片转换（Gemini 等 vendor_b 模型）

```javascript
function convertBase64ToBlobUrl(dataUrl) {
    if (!dataUrl || !dataUrl.startsWith('data:')) return dataUrl;
    try {
        const parts = dataUrl.split(',');
        const mime = parts[0].match(/:(.*?);/)?.[1] || 'image/png';
        const bstr = atob(parts[1]);
        const n = bstr.length;
        const u8arr = new Uint8Array(n);
        for (let i = 0; i < n; i++) u8arr[i] = bstr.charCodeAt(i);
        const blob = new Blob([u8arr], { type: mime });
        return URL.createObjectURL(blob);
    } catch (err) {
        console.error('Base64→Blob 转换失败:', err.message);
        return dataUrl;
    }
}
```

### 7.2 Token Switch 受保护视频获取

```javascript
async function fetchProtectedVideo(videoUrl) {
    console.log('🔒 [TokenSwitch] 正在获取受保护视频:', videoUrl);
    try {
        const response = await fetch(videoUrl, {
            method: 'GET',
            headers: { Authorization: `Bearer ${VENDOR_B_API_KEY}` }
        });
        if (!response.ok) throw new Error(`HTTP ${response.status}`);
        const blob = await response.blob();
        return URL.createObjectURL(blob);
    } catch (err) {
        console.error('🔒 [TokenSwitch] 获取视频失败:', err.message);
        throw err;
    }
}
```

### 7.3 主渲染函数（异步）

```javascript
async function renderTaskResult(loadingElem, taskResult, sceneType) {
    const result = taskResult.result || {};

    function extractVideoUrl(r) {
        return r.video_url || (r.video && r.video.url) ||
               r.url || r.output_url || r.file_url || null;
    }

    // ===== 视频/数字人场景 =====
    if (sceneType === 'video' || sceneType === 'digital-human') {
        const videoUrl = extractVideoUrl(result);
        if (videoUrl) {
            addToHistory({ type: 'video', url: videoUrl, name: taskResult.task_id });
            const currentVendor = window.__selectedModelVendor || '';

            // vendor_b 需要携带 Authorization 获取 blob
            if (currentVendor === 'vendor_b') {
                try {
                    const blobUrl = await fetchProtectedVideo(videoUrl);
                    updateLoadingToResult(loadingElem, 'video', {
                        feedback: feedbackText, videoUrl: blobUrl
                    });
                } catch (err) {
                    updateLoadingToResult(loadingElem, 'error', {
                        message: `TokenSwitch视频获取失败: ${err.message}`
                    });
                }
            } else {
                updateLoadingToResult(loadingElem, 'video', {
                    feedback: feedbackText, videoUrl: videoUrl
                });
            }
            return;
        }
    }

    // ===== 图片场景（自动转换 base64）=====
    if (Array.isArray(result.images)) {
        result.images.forEach(img => {
            const rawUrl = img.url || img;
            const finalUrl = convertBase64ToBlobUrl(rawUrl);
            addToHistory({ type: 'image', url: finalUrl, name: img.id || '' });
        });
        updateLoadingToResult(loadingElem, 'image', {
            feedback: feedbackText,
            images: result.images.map(img => convertBase64ToBlobUrl(img.url || img))
        });
        return;
    }

    // ===== 回退：尝试提取视频URL =====
    const fallbackVideoUrl = extractVideoUrl(result);
    if (fallbackVideoUrl) {
        // ... 同上 vendor_b 处理逻辑 ...
    }
}
```

---

## 8. 模型选择交互

### 8.1 获取选中模型信息

```javascript
function getSelectedModelId() {
    const visibleScene = getVisibleSceneEl();
    const activeModel = visibleScene
        ? visibleScene.querySelector('.model-option.active')
        : document.querySelector('.model-option.active');

    if (activeModel) {
        const nameEl = activeModel.querySelector('.model-name');
        const modelId = activeModel.getAttribute('data-model-id') || '';
        const modelVendor = activeModel.getAttribute('data-vendor') || '';
        let modelName = '';
        if (nameEl) {
            modelName = nameEl.childNodes[0]?.textContent?.trim() || nameEl.textContent.trim();
        }
        if (!modelName) modelName = modelId || activeModel.textContent.trim();

        // 存储到全局变量供 buildGenerateRequest 使用
        window.__selectedModelVendor = modelVendor;
        window.__selectedModelId = modelId;
        window.__selectedModelName = modelName;

        console.log('🎯 已选择模型:', {
            name: modelName, id: modelId,
            vendor: modelVendor || '(无)',
            submitAs: (modelVendor === 'vendor_b'
                ? 'id(' + modelId + ')'
                : 'name(' + modelName + ')')
        });
        return modelName;
    }
    // fallback...
}

function getCurrentModelName() {
    const visibleScene = Array.from(document.querySelectorAll('.scene-controls'))
        .find(s => window.getComputedStyle(s).display !== 'none');
    let activeModel = visibleScene?.querySelector('.model-option.active')
        || document.querySelector('.model-option.active');
    if (!activeModel) {
        const dropdownLabel = document.querySelector('.control-dropdown.model-dropdown span');
        return dropdownLabel ? dropdownLabel.textContent.trim() : '大模型';
    }
    const nameEl = activeModel.querySelector('.model-name');
    return nameEl ? nameEl.textContent.trim() : activeModel.textContent.trim();
}
```

### 8.2 场景类型判断

```javascript
function getActiveSceneType() {
    const scene = Array.from(document.querySelectorAll('.scene-controls'))
        .find(s => window.getComputedStyle(s).display !== 'none');
    if (!scene) return 'image';
    if (scene.classList.contains('video-scene')) return 'video';
    if (scene.classList.contains('digital-human-scene')) return 'digital-human';
    return 'image';
}
```

### 8.3 模型渲染（厂商分组）

```javascript
function renderImageModels(models) {
    const imageModelDropdown = document.querySelector('#imageModelDropdown .dropdown-menu');

    // 按厂商分组
    const vendorGroups = {};
    models.forEach(m => {
        const v = m.vendor || 'unknown';
        if (!vendorGroups[v]) vendorGroups[v] = [];
        vendorGroups[v].push(m);
    });

    Object.entries(vendorGroups).forEach(([vendorId, groupModels]) => {
        const groupName = groupModels[0]?.vendor_name || vendorId;
        // 添加厂商标题头
        const groupHeader = document.createElement('div');
        groupHeader.className = 'model-vendor-group';
        groupHeader.innerHTML = `<span class="vendor-label">${groupName}</span>`;
        imageModelDropdown.appendChild(groupHeader);

        // 渲染每个模型选项
        groupModels.forEach(model => {
            const modelOption = document.createElement('div');
            modelOption.className = 'model-option';
            modelOption.setAttribute('data-model-id', model.id);
            modelOption.setAttribute('data-vendor', model.vendor || '');

            const badgeHtml = model.is_new ? '<span class="model-badge">New</span>' : '';
            const vipBadge = model.is_vip ? '<span class="model-badge-diamond">&#10022;</span>' : '';
            const freeBadge = model.free_trial ? '<span class="model-badge-special">限免1次</span>' : '';

            modelOption.innerHTML = `
                <div class="model-icon"><i class="fas fa-paper-plane"></i></div>
                <div class="model-info">
                    <div class="model-name">${model.name}${badgeHtml}${vipBadge}${freeBadge}</div>
                    <div class="model-desc">${model.description || ''}</div>
                </div>
            `;
            imageModelDropdown.appendChild(modelOption);
        });
    });

    bindModelOptionEvents();  // 绑定点击事件
}
```

---

## 9. HTML 结构参考

### 9.1 上传区域

```html
<!-- 上传按钮 -->
<button id="uploadBtn" class="upload-btn">
    <i class="fas fa-plus-circle"></i> 上传素材
</button>

<!-- 上传下拉菜单 -->
<div id="uploadMenu" class="upload-menu">
    <div class="dropdown-item" data-action="local">
        <i class="fas fa-folder-open"></i> 本地文件上传
    </div>
    <div class="dropdown-item" data-action="history">
        <i class="fas fa-history"></i> 历史生成文件
    </div>
</div>

<!-- 隐藏的文件 input -->
<input type="file" id="localFileInput" accept="image/*,video/*,audio/*" multiple hidden>

<!-- 文件预览容器 -->
<div id="uploadedFilesPreview" class="file-previews"></div>
```

### 9.2 历史文件弹窗

```html
<div id="historyModalOverlay" class="modal-overlay" style="display:none;">
    <div class="modal-content">
        <div class="modal-header">
            <h3>选择历史文件</h3>
            <button id="historyModalClose">&times;</button>
        </div>
        <div id="historyModalBody" class="modal-body"></div>
        <div class="modal-footer">
            <button id="historyConfirmBtn" class="btn-primary">确认选择</button>
        </div>
    </div>
</div>
```

### 9.3 对话区域

```html
<div class="chat-container">
    <!-- 消息列表 -->
    <div class="chat-messages"></div>

    <!-- 输入栏 -->
    <div class="chat-input-area">
        <textarea class="chat-input" placeholder="描述你想要生成的内容..."></textarea>
        <button class="send-btn">
            <i class="fas fa-paper-plane"></i>
        </button>
    </div>
</div>
```

### 9.4 模型选择器

```html
<div class="control-group">
    <label>选择模型</label>
    <div class="control-dropdown model-dropdown" id="imageModelDropdown">
        <span>-- 请选择 --</span>
        <i class="fas fa-chevron-down"></i>
        <div class="dropdown-menu">
            <div class="model-panel-header">请选择模型</div>
            <!-- 动态填充的模型选项 -->
        </div>
    </div>
</div>
```

---

## 迁移检查清单

- [ ] 复制 API 配置和常量定义
- [ ] 实现 initFileUpload() 函数
- [ ] 实现 initSendButton() 函数
- [ ] 实现 buildGenerateRequest() 函数
- [ ] 实现渲染相关函数（renderTaskResult、updateLoadingToResult）
- [ ] 实现 Base64 转 Blob 函数
- [ ] 实现 Token Switch 受保护视频获取
- [ ] 配置对应的 HTML 元素结构
- [ ] 引入 CSS 样式文件
